input {
    file {
        path => "/home/cloud_user/elk/data/*.csv"
        start_position => "beginning"
        mode => "read"
        file_completed_action => "log_and_delete"
        file_completed_log_path => "/home/cloud_user/elk/logs/udemy-log2.log"
    }
}
filter {
    csv {
        columns => ["product", "stock", "price"]
        skip_header => true
        convert => {
          "stock" => "integer"
          "price" => "float"
        }
    }
}
output {
    stdout { }
    elasticsearch {
       hosts => ["https://logstash:passwd123@172.31.31.246:9200"]
       ssl_certificate_authorities => "/home/cloud_user/http_ca.crt"
       ssl_verification_mode => none
       ssl_enabled => true
       index => "udemy-log2"
       doc_as_upsert => true
    }
}
